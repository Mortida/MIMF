# Metadata Intelligence & Mutation Framework (MIMF) — v1.0.0

MIMF is a CLI + API for inspecting files, normalizing metadata, and producing tamper-evident forensic export bundles with optional chain-of-custody events.

## Install
```bash
uv sync

